services:
  - type: web
    name: scoutsearch
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn Backend.src.app:app --workers 1 --threads 2 --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    autoDeploy: true
